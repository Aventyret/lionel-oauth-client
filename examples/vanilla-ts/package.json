{
  "name": "lionel-oauth-client-example-vanilla-ts",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prebuild": "cd ../../ && yarn && yarn build",
    "predev": "yarn prebuild"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  }
}
