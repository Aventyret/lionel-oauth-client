{
  "private": true,
  "name": "lionel-oauth-client-monorepo",
  "author": "Äventyret Sweden AB",
  "version": "1.0.0",
  "description": "Monorepo for Linonel oAuth client",
  "scripts": {
    "build": "pnpm --stream -r run build",
    "dev": "pnpm --stream -r run dev",
    "tag": "./tag.sh",
    "lint": "pnpm --stream -r run lint",
    "husky": "husky install",
    "pre-commit": "lint-staged",
    "test": "pnpm --stream -r run test",
    "test:unit": "pnpm --stream -r run test:unit",
    "test:e2e": "pnpm --stream -r run test:e2e"
  },
  "keywords": [],
  "license": "MIN",
  "devDependencies": {
    "eslint": "^8.11.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^7.0.4",
    "jest": "^27.5.1",
    "lint-staged": "^12.3.5",
    "prettier": "^2.5.1"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write"
    ],
    "*.ts?(x)": [
      "prettier --parser=typescript --write",
      "eslint"
    ]
  }
}
